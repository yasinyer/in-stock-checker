{
  "name": "in-stock-checker",
  "version": "1.0.0",
  "private": true,
  "description": "Checks Uniqlo and COS product pages for stock availability and sends push notifications via ntfy.sh.",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "check": "node src/check-stock.mjs"
  },
  "dependencies": {
    "playwright": "^1.45.0"
  }
}
